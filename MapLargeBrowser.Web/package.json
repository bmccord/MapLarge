{
  "name": "maplargebrowser-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vanilla TypeScript SPA for the MapLargeBrowser test project.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --port=5081",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "lucide-static": "^1.17.0",
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
